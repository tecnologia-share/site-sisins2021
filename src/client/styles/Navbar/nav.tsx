import { StyledNav } from '../Navbar/nav-style';
import Logo from '../Navbar/Nav-items/Logo/logo';
const Nav = () => {
  return (
    <StyledNav>
      <Logo />
      Teste
    </StyledNav>
  );
};

export default Nav;
